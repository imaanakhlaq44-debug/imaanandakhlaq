const fs = require('fs'); ['ActivityDashboard.tsx', 'ParentDashboard.tsx', 'ActivityPage.tsx'].forEach(file => { try { const content = fs.readFileSync('src/components/' + file, 'utf8'); const m = content.match(/<script type=.module.>([\s\S]*?)<\/script>/); if(m) { try { const vm = require('vm'); const code = m[1].replace(/import .*? from .*?;/g, ''); new vm.Script(code); console.log(file + ' OK'); } catch(e) { console.log(file + ' ERROR: ' + e); } } } catch(e) {} });
